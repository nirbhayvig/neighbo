# Port the Hono API server listens on
PORT=3001

# Firebase Admin SDK — provide one of the following:
# Option A: Path to your downloaded service account JSON file
GOOGLE_APPLICATION_CREDENTIALS=./service-account.json

# Option B: Application Default Credentials (set automatically on Cloud Run / GCE)
# Leave GOOGLE_APPLICATION_CREDENTIALS unset and ADC will be used.

# Your Firebase project ID
FIREBASE_PROJECT_ID=your-project-id

# Allowed CORS origin (your web app URL)
CORS_ORIGIN=http://localhost:5173
